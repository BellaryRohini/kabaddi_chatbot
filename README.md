# Kabaddi Chatbot 🏏🤖

A console-based Kabaddi chatbot built using Python and TF-IDF similarity.  
Users can ask Kabaddi-related questions and get instant answers based on the provided dataset.

## Tech Stack
- Python
- scikit-learn
- TF-IDF Vectorizer
- Cosine Similarity

## Features
- Console interface for user queries
- Matches questions using TF-IDF and cosine similarity
- Returns the most relevant answer from the dataset

## How to Run
1. Install dependencies:
   ```bash
   pip install scikit-learn
